<div>
    <div class="row">
     <span class="col-3"></span> 
    <span class="col-5">{{id}} {{burger_name}}</span>
    {{!-- <span class="col-3"></span> --}}
      {{#unless devoured}}
        <span class="col-3"><button class="devour-it" data-id="{{id}}"> Devour It!</button></span>
        <span class="col-1"></span>  
      {{/unless}}
      </div>
</div>